"""xpkgindex — a static site framework for package indexes.

The core is ecosystem-agnostic; everything that knows what a package *means*
in a particular ecosystem lives in a plugin owned by the index repository.
"""

__version__ = "0.2.0"
