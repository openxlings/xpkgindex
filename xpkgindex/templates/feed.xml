<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ config.title | loc }}</title>
  <link href="{{ base }}/{{ page_suffix }}"/>
  <link rel="self" href="{{ base }}/feed.xml"/>
  <id>{{ base }}/</id>
  <subtitle>{{ config.description | loc }}</subtitle>
{% for ev in events %}
  <entry>
    <title>{{ ev.kind }}: {{ ev.display }}</title>
    <id>{{ base }}/packages/{{ ev.slug }}/#{{ ev.date }}-{{ loop.index }}</id>
    <link href="{{ base }}/packages/{{ ev.slug }}/{{ page_suffix }}"/>
    <updated>{{ ev.date }}T00:00:00Z</updated>
    <summary>{{ ev.subject or ev.kind }}{% if ev.by %} — {{ ev.by }}{% endif %}</summary>
  </entry>
{% endfor %}
</feed>
